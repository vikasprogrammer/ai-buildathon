# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*.swo

# Claude Code local config (contains conversation state, not for sharing)
.claude/

# Arduino build artifacts
build/
*.hex
*.elf
*.bin

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Camera runtime data — NEVER commit this.
# Holds doorway photos, cropped faces, and face embeddings of real
# people. This repo is public; biometric data does not belong in it.
camera/data/
